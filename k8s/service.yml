apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  type: LoadBalancer
  # localmente o gateway é NodePort!!!
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080

  selector:
    app: gateway